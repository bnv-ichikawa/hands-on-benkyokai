# CONSTITUTION

このファイルは、**このリポジトリ全体にかかる決まりの正本**です。

個別の作業手順は [`.agents/skills/`](./.agents/skills/)、Second Brain の考え方は [`docs/`](./docs/README.md)、各ディレクトリの使い方はそのディレクトリの `README.md` にあります。ここには、**それら全部にかかる決まり**だけを置きます。

### 1. 正本と案内を分ける

**決まりの本体を置くファイルを1つ決め、そこを正本とする。他のファイルは正本を指し示すだけにする。**

**案内とは、リンクと、それが何かを示す1行である。案内は複製ではない。** 入口の `README.md`、ディレクトリの一覧表、MOC は案内であり、正本ではない。

判断基準そのもの、閾値、手順、禁止事項を書いたら、それは案内ではなく**本体**である。**本体は正本にだけ置く。**

> **なぜこの原則を置くのか。** 案内と本体の区別がないと、入口が本体を抱え始める。このリポジトリでは `docs/` の一覧が `README.md`、`docs/README.md`、`second-brain/README.md` の3箇所にあり、`docs/` にファイルを1つ足すと3箇所を直すことになっている。**案内なら1行で済み、本体なら1箇所で済む。増えるのは、混ざったときだけである。**

### 2. AIが読むものと、人間が読むものを混ぜない

**AIへの指示と、人間への説明を同じファイルに置かない。**

| 置き場所 | 誰が読むか | 何を置くか |
| --- | --- | --- |
| `AGENTS.md` | AI | いま効いている人格の定義。ここだけが正本 |
| `.agents/skills/` | AI | 作業の進め方の指示 |
| `README.md` / `docs/` | 人間 | 構成の案内と、考え方の説明 |

> **なぜこの原則を置くのか。** 混ざると、AIが読む量が呼び出しごとに変わる。[teach-my-work](./.agents/skills/teach-my-work/SKILL.md) は「キャラクター定義は `AGENTS.md` へ展開済みなので、追加で読むファイルはない」を前提に動いている。**説明が指示に混ざった時点で、この前提は黙って壊れる。**

### 3. 埋まっていない箇所を、埋まったように見せない

**分からないことを推測で埋めない。** 未確認のまま残し、**誰に聞けば埋まるか**を書く。途中までしか終わっていないなら、そう伝える。

> **なぜこの原則を置くのか。** [teach-my-work](./.agents/skills/teach-my-work/SKILL.md) はこれを到達基準として持っているが、**skill の中だけの決まりだと、skill を通らない成果物には効かない。** `README.md`、`docs/`、`tools/` も同じ基準で扱う。穴が空いていると分かる状態のほうが、埋まったふりより価値がある。

### 4. 認証情報と個人情報をリポジトリに入れない

パスワード、APIキー、アクセストークン、顧客や同僚の個人情報を置かない。**自分の氏名やメールアドレスも、公開リポジトリへ入れる前に一度止まって確認する。**

**`.gitignore` は未追跡のファイルにしか効かない。** 一度コミットしたファイルは、`.gitignore` に書いてあっても追跡され続ける。

> **なぜこの原則を置くのか。**

### 5. 特定のPCでしか動かない状態を作らない

**個人PCの絶対パスをコミットしない。** 必要なランタイムとバージョンを明記する。依存関係は再現できる形で残す。

手元の絶対パスでなければ動かないものは、**正本にはプレースホルダーを置き、置き換えは各自の手元で行う。** キャラクター画像の参照がこの形になっている。

> **なぜこの原則を置くのか。** 絶対パスはPCごとに違う。正本に1つ書いた時点で、そのファイルは配れなくなる。

### 6. 1つの決まりの正本は1つのファイルに置く

**同じ決まりを複数のファイルへ書かない。他のファイルからは参照する。** **言い換えて再掲することも複製に含める。** 表現を変えても、正本と食い違う余地は同じだけ生まれる。

複製してよいのは、**参照にできない構造的な理由がある場合だけ**とする。そのときは次の両方を満たす。

1. **複製してよい対象を、[tools/repo-checks](./tools/repo-checks) の登録一覧へ書く。** どこを複製しているかの記録は、その一覧を唯一の場所とする
2. **一致を機械的に検査する仕組みを、同じ場所に置く**

**検査を置けないなら、複製しない。**

> **なぜこの原則を置くのか。** 同じ判断基準を5つのファイルへ言い換えて書いた変更で、「5箇所のうち1箇所だけ古い」という指摘がレビュー3周にわたって出た。**5箇所が揃っていることを検査する仕組みが無かったため、食い違いは事故ではなく既定の結果だった。**

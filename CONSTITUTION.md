# CONSTITUTION

このファイルは、**このリポジトリ全体にかかる決まりの正本**です。

個別の作業手順は [`.agents/skills/`](./.agents/skills/)、リポジトリと勉強会に必要な人間向けドキュメントは [`docs/`](./docs/README.md)、各ディレクトリの使い方はそのディレクトリの `README.md` にあります。ここには、**それら全部にかかる決まり**だけを置きます。

### 1. 正本と案内を分ける

**決まりの本体を置くファイルを1つ決め、そこを正本とする。他のファイルは正本を指し示すだけにする。**

**案内とは、リンクと、それが何かを示す1行である。案内は複製ではない。** 入口の `README.md`、ディレクトリの一覧表、MOC は案内であり、正本ではない。

判断基準そのもの、閾値、手順、禁止事項を書いたら、それは案内ではなく**本体**である。**本体は正本にだけ置く。**

`README.md` を作成・更新するときは [docs/readme-authoring-rules.md](./docs/readme-authoring-rules.md) に従う。

> **なぜこの原則を置くのか。** 同じ内容が複数の場所に書かれていると、更新によって内容が食い違い、どれが正しい情報か分からなくなる。正本を1か所に決め、他のファイルはそこを案内するだけにすることで、参照すべき情報を明確にし、更新箇所も最小限にできる。

### 2. 1つの決まりの正本は1つのファイルに置く

**同じ決まりを複数のファイルへ書かない。他のファイルからは参照する。** **言い換えて再掲することも複製に含める。** 表現を変えても、正本と食い違う余地は同じだけ生まれる。

**ある決まりの正本を別のファイルへ移したら、指す側はリンクだけにする。** **条件・要約・言い換えを添えない。** 「詳細はこちら」で止める。**一言添えた瞬間に、それが二重管理になる。**

複製してよいのは、**参照にできない構造的な理由がある場合だけ**とする。そのときは次の両方を満たす。

1. **複製してよい対象を、[tools/repo-checks](./tools/repo-checks) の登録一覧へ書く。** どこを複製しているかの記録は、その一覧を唯一の場所とする
2. **一致を機械的に検査する仕組みを、同じ場所に置く**

**検査を置けないなら、複製しない。**

> **なぜこの原則を置くのか。** 同じ判断基準を5つのファイルへ言い換えて書いた変更で、「5箇所のうち1箇所だけ古い」という指摘がレビュー3周にわたって出た。**5箇所が揃っていることを検査する仕組みが無かったため、食い違いは事故ではなく既定の結果だった。**
>
> **リンクに一言添えるのも同じである。** 正本を別のファイルへ切り出した変更で、移設先を指すリンクへ条件文を1つ添えたことがレビュー指摘になった。**その条件をリンク先へ寄せた次の周で、指摘は0になった。**

### 3. ファイルの書き換えは編集専用のツールで行う

**`sed`、`awk`、`printf` などのシェルコマンドでファイルを書き出したり置換したりしない。** ファイル編集専用のツールを使う。

> **なぜこの原則を置くのか。** シェルの置換は、エスケープの解釈で壊れる。`sed` の置換文字列に含まれる `\U` は大文字化の指示として解釈され、日本語の1行が文字化けした。複数行を挿入したときは改行が潰れ、`if` 文がコメントの内側に入って検査の免除が黙って無効になった。空の変数を渡した `awk` は、README を0行にした。**いずれも、実行した時点では成功したように見える。**
